$("#<%= @card_row_id %>").find(".suggest-answer").html(`
  <% @cards.each do |card| %>
    <button class="btn btn-outline-primary mt-2 suggest-word">
        <%= card.answer %>
    </button>
  <% end %>
`)
$("#<%= @card_row_id %>").find(".suggest-answer").show();

