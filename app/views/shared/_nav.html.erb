<nav class="navbar navbar-expand-lg navbar-dark bg-primary" style="height: 4rem">
  <div class="container">
    <% if current_user %>
      <%= link_to "iAnki", home_path, class: "navbar-brand" %>
    <% else %>
      <%= link_to "iAnki", root_url, class: "navbar-brand" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item ml-5">
          <%= form_tag(search_path, method: 'get', class: "form-inline") do %>
            <%= text_field_tag :query, nil, class: "form-control mr-sm-2", placeholder: (t :enter_key_word)  %>
            <%= submit_tag "Search", name: nil, class: "btn btn-outline-light my-2 my-sm-0" %>
          <% end %>
        </li>
        <li class="ml-3">
          <%= link_to t(:create), new_deck_path, class: "nav-link nav-link-white" %>
        </li>
      </ul>
      <nav class="nav">
        <% if current_user %>
          <%= user_image current_user, class: "rounded-circle", style: "width: 40px; height:40px;"%>
          <div class="btn-group">
            <button type="button" class="ml-1 btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= current_user.name %>
            </button>
            <div class="dropdown-menu">
              <%= link_to t(:profile), user_path(current_user), class: "dropdown-item"  %>
              <%= link_to t(:log_out),logout_path, method: :delete, class: "dropdown-item"  %>
            </div>
          </div>
        <% else %>
          <%= link_to '🇬🇧', root_path, class: "btn btn-primary", style: "font-size: 30px" %>
          <%= link_to '🇯🇵', landing_path(:jp), class: "btn btn-primary", style: "font-size: 30px" %>
          <button type="button" id="buttonLoginModal" class="btn btn-primary" data-toggle="modal" data-target="#loginModal">
            <%= t :login %>
          </button>
          <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#signUpModal">
            <%= t :sign_up %>
          </button>
        <% end %>
      </nav>
    </div>
  </div>
</nav>